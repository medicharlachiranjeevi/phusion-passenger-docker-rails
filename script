port=3000
while true;
do
if sudo netstat -anp | grep ":$port "  ; then 
port=`echo "$port + 1 " | bc`
echo $port


else

export port=$port;
echo $port
break

fi

done
echo $port
sed -i "s/80/$(port)/g" .env
sudo docker-compose -f devdocker-compose.yaml up  -d

